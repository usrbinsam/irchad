# chadd

The Chad Daemon

## Setup

`chadd` needs the following environment variables to operate:

- `CHADD_LIVEKIT_KEY`
- `CHADD_LIVEKIT_SECRET`
- `CHADD_ERGO_BEARER_TOKEN`
- `CHADD_ERGO_BASE_URL`
- `CHADD_AUTH_SECRET`

Mount a `/config.json` for IrChad auto discovery
